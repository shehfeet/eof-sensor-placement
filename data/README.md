# Data files

Place the NetCDF input files in this directory.

Default filenames:

- `topo.nc`
- `3D_mean_2016-03.nc`

Other filenames or locations can be used with the `--topo` and `--data` command-line options.

NetCDF files are configured for Git LFS in the repository `.gitattributes` file.
